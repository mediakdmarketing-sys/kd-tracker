# Where the backend API lives. Server-side only — this value is never sent to the browser.
API_BASE_URL=http://localhost:4000

# Set to 1 behind HTTPS so session cookies are marked Secure.
# COOKIE_SECURE=1
